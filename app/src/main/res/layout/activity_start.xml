<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical">

        <Button
            android:id="@+id/retainable"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:text="Retainable Activity" />

        <Button
            android:id="@+id/savable"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:text="Savable Activity" />

        <Button
            android:id="@+id/pagingActivity"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:text="Paging Activity" />

        <Button
            android:id="@+id/fragments"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:text="Fragments" />

        <Button
            android:id="@+id/pagingFragment"
            android:layout_width="180dp"
            android:layout_height="wrap_content"
            android:text="Paging Fragment" />

    </LinearLayout>

</FrameLayout>